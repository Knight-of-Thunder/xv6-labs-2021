# mit6.s081操作系统实现
每个lab对应一个单独的分支
该分支已实现的内容
- RISC-V assembly（文字答案，本分支中未提交）
- Backtrace

1. 为什么从用户态trap进入内核的时候要把寄存器的值存进进程的trapframe里面，
返回用户态的时候又要把寄存器的值从trapframe里回寄存器里面去，不能就一直保留在
寄存器里面吗

不能，有以下原因：
1. trap后内核执行处理trap的程序时有可能修改寄存器的值
2. 多线程之间切换的时候，一个新的进程重新被调度器选择开始执行，此时该cpu的
寄存器值是上一个进程使用的，需要从tf里获取寄存器相关的上下文。
